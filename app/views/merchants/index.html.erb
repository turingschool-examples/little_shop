<% @merchants.each do |merchant| %>
  <section id='merchant-<%= merchant.id %>'>
    <h2><%= link_to merchant.name, merchant_path(merchant) %></h2>
  </section>
<% end %>
<br/>

<section class='nav-2'>
  <%= link_to 'New Merchant', '/merchants/new', class: 'link-btn' %>
</section>
